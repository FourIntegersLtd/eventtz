# Backend (FastAPI / Uvicorn). No trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Display only — keep in sync with backend BOOKING_SERVICE_FEE_PERCENT (default 5)
NEXT_PUBLIC_BOOKING_SERVICE_FEE_PERCENT=5

